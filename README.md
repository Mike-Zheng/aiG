# aiG

## 1. Install dependencies

```bash
npm install
```

## 2. Prepare WebP animations

Place `.webp` animation files in the `assets/` directory

## 3. Build the database

```bash
npm run build
```

Generates the `dist/data.bin` file